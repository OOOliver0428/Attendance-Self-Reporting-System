import pymysql

host = '43.140.253.7'
user = "root"
password = "Shy990428!"
database = "shy"
port = 22704  # 用您自己的端口号替换

'''try:
# 连接到 MySQL 服务器
    connection = pymysql.connect(
        host=host,
        user=user,
        password=password,
        database=database,
        port=port
    )
    print("连接成功！")

except pymysql.Error as e:
    print(f"连接失败，错误信息：{e}")

finally:
    # 关闭连接
    if connection:
        connection.close()'''
        
        
